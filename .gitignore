node_modules/
.next/
out/
next-env.d.ts
*.tsbuildinfo
.DS_Store
.vercel/

# Derived data: generated by `npm run scrape`, deployed to Pages, never committed.
# (The builder reads prior state back from the live site, so git stays free of it.)
/public/index.json
/public/feed.json
/public/feed.xml
/public/members.opml
